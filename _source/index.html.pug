extends /_source/_layouts/prime
block append style
  style.
    ._nav { display:none !important }
block footer
  +thin_hr
  p: +site_footer
  +thin_hr
  p.text-right.text-grey: small: +edit_link(path)
//- --------------------------------------------------------------
block append vars
  - path   = 'index.html'
  - title  = `Index`
block append style
  style.
    h1.hero { font-family: 'Arial',system-ui,sans-serif }
block content
  h1.hero 🕊️ !{description}
  pre.scrollable.highlight(style="max-height:235px").
    #[b.text-gray # _config.pug]
    version      : !{version}
    revision     : !{revision}
    title        : !{site_title}
    description  : !{site_description}
    author       : !{author}
    year         : !{year}
    base_url     : #[a._no_u(href=base_url)|!{base_url}]
    home_url     : #[a._no_u(href=home_url)|!{home_url}]
    ghost        : !{ghost}

    google_analytics     : !{google_analytics||'(undefined)'}
    cloudflare_analytics : !{cloudflare_analytics||'(undefined)'}

    source_url   : #[a._no_u(href=source_url target="_edit") !{source_url}]
  nav: p
      +_a([live_url,'md'].join('/'))(target="_blank")| !{site_title}.md
      +_bull
      +_a('https://techie-joe.github.io/proto')(target="_blank")| Proto
  nav: ul
    li: +site_nav('core')
    li: +site_nav('mallet')
    li: +site_nav('nova')
    li: +site_nav('prime')