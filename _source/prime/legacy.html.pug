extends _html
block append vars
  - path   = 'legacy.html'
  - title  = `Legacy`
block content
  +capture('legacy')|{% include legacy.md %}
  +l|{{ legacy | markdownify }}
block footer
  +thin_hr
  p.text-right.text-grey: small: +edit_link(path)
